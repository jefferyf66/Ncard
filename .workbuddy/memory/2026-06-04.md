# 2026-06-04 工作日志

## 编辑页头像上传功能修复（5轮迭代）
1. **第一轮**：API 层 - `wx.showActionSheet` + `wx.chooseImage` → `wx.chooseMedia`
2. **第二轮**：DOM 层 - `avatar-mask`/`mask-icon` 加 `bindtap`，防止绝对定位溢出导致 tap 丢失
3. **第三轮**：`<view>` → `<button>` + 调试吐司，真机验证 tap 到达 JS
4. **第四轮**：`<button>` → `<view catchtap>` + 回归 `showActionSheet` → `chooseImage`，button 干扰隐私 API
5. **第五轮**：新增 `_ensurePrivacyAuth()` 隐私授权流程（`wx.getPrivacySetting` → `wx.requirePrivacyAuthorize`），基础库 2.32.3+ 隐私授权兼容

## 发布准备：删除 mock 数据 + 完善数据库连接

### Mock 数据清理（3处）
1. **`miniprogram/pages/index/index.js`**：
   - `visitorStats` 硬编码测试值 → 全部归零
   - `recentVisitors` 3 条模拟访客 → 空数组
   - 新增 `loadVisitorData()` 方法，从 `visits` 集合查询真实数据
2. **`miniprogram/pages/visitors/index.js`**：
   - 3 条模拟访客（含硬编码手机号 18502925292）→ 空数组
   - `stats` 测试值 → 全部归零
   - 重写 `loadVisitors()`：`setTimeout` 假 loading → 真实 `visits` 集合查询
3. **`cloudfunctions/parseCard/index.js`**：
   - 删除 OCR 失败时的 mock 回退数据（"张三\n产品经理\n腾讯科技..."）→ 直接返回错误

### 文件清理（10项删除）
- 2 个同步冲突文件
- `miniprogram/pages/example/` 整个示例目录
- 3 个设计 HTML（design-directions, design-hybrid, ui-preview）
- `uploadCloudFunction.sh` 开发脚本
- `cloudfunctions/quickstartFunctions/` 模板云函数
- `.workbuddy/memory/*.md` 旧记忆文件

### 配置修正
- `app.json`: `navigationBarBackgroundColor` #165DFF → #3B82F6
- `README.md`: 模板内容 → 实际项目文档

### 数据库连接确认
- 云开发已正确初始化（DYNAMIC_CURRENT_ENV）
- 现有云函数：getOpenId, getQrCode, parseCard
- 数据集合：cards, scans, visits
- 各页面均已对接真实云数据库查询
